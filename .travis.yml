language: python

python:
  - "2.7"

env:
  global
    - PYTHONPATH="/home/travis/build/joacorapela/gaussianProcesses/src/gaussianProcesses"

install:
  - pip install -r requirements.txt

script:
  - pytest
